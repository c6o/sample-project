apiVersion: v1
kind: Service
metadata:
  name: sample-project-server
  labels:
    app: sample-project
    component: server
spec:
  ports:
    - name: unsecure
      protocol: TCP
      port: 3000
      targetPort: 3000
  selector:
    app: sample-project
    component: server