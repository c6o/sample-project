apiVersion: v1

kind: Service

metadata:
  name: halyard-backend-service
  labels:
    app: halyard-backend-service
    
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  selector:
    app: halyard-backend-pod
