
apiVersion: system.codezero.io/v1
kind: Environment
metadata:
  name: prod
spec:
  services:
    halyard-database:
      secretsRef:
      - halyard-database-connections
    halyard-backend: ../apps/halyard-backend.yaml
    halyard-frontend: ../apps/halyard-frontend.yaml
