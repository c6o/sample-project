# To Robb - with Love
# An approximation of this can be generated by running
# czctl install ./c6o/backend.yaml --local --spec-only
apiVersion: system.codezero.io/v1
kind: App
metadata:
  name: halyard-backend
  labels:
    app.kubernetes.io/version: 0.0.1
  annotations:
    system.codezero.io/display: Halyard Backend
spec:
  provisioner:
    package: '@provisioner/appengine'
    image: c6oio/halyard-sample-backend:narayans
    tag-prefix: appengine
    ports: 3000
